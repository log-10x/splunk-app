
# Lookup definition for getting processed 10x pattern parts needed for decoding 10x encoded events.
#
# See macros.conf for more info.
#
[tenx-dml-lookup]
case_sensitive_match = true
collection = tenx_dml
external_type = kvstore
fields_list = _key,pattern_hash,pattern,pattern_parts,part_0,pattern_terminator,timestamp_format


# Extraction on encoded 10x events to prep them for decoding.
#
# See macros.conf for more info.
#
# Format: ~<hash>,<var0>,<var1>,...  (comma-delimited with ~ prefix)
# Hash may contain spaces and special characters, so we only split on commas
#
[tenx-hash-vars-extraction]
REGEX = ^~?(?<tenx_hash>[^,]+),(?<tenx_var_0>[^,]+)(?:,(?<tenx_vars>.*))?
